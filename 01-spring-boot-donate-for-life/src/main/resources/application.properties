spring.application.name=demo

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/projectD4L?serverTimezone=Asia/Kolkata
spring.datasource.username=springstudent
spring.datasource.password=springstudent

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=validate
#spring.jpa.show-sql=true # You can keep this commented out or remove it
spring.jpa.properties.hibernate.format_sql = true # This is good for readable SQL logs
spring.jpa.open-in-view=false

# Flyway
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# --- SendGrid Email Configuration ---
# Used by EmailService for Admin Notifications (Donor Request, Donation Reject)
# SENDGRID_API_KEY MUST be set as an environment variable
sendgrid.api.key=${SENDGRID_API_KEY}
app.sender.email=mohityadav950410s@gmail.com

# --- Spring Mail Configuration ---
# Used by UserService (JavaMailSender) for sending OTP emails via Gmail
# GMAIL_APP_PASSWORD MUST be set as an environment variable
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mohityadav950410s@gmail.com
spring.mail.password=${GMAIL_APP_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true