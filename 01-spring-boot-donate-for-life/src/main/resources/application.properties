spring.application.name=demo

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/projectD4L?serverTimezone=Asia/Kolkata
spring.datasource.username=springstudent
spring.datasource.password=springstudent

# JPA / Hibernate
# Let Flyway manage the schema, and have Hibernate validate it.
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# Flyway
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# SendGrid Email Configuration
# These values MUST be set as environment variables in your run configuration
sendgrid.api.key=${SENDGRID_API_KEY}
app.sender.email=mohityadav950410s@gmail.com

# Google Maps API Key
google.maps.api.key=${GOOGLE_MAPS_API_KEY}


# Spring Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mohityadav950410s@gmail.com
spring.mail.password=${GMAIL_APP_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
